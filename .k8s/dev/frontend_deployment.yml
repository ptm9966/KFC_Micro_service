apiVersion: apps/v1
kind: Deployment
metadata:
  name: kfc-frontend
  namespace: kfc-dev
  labels:
    app: kfc-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kfc-frontend
  template:
    metadata:
      labels:
        app: kfc-frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/ptm9966/kfc-frontendmicorservice:4986f730344ef4c7548298150f391bb37733b6cf
        ports:
        - containerPort: 80
        env:
        # Backend URL is empty - frontend will use same origin (/api proxy in nginx)
        - name: REACT_APP_BACKEND_URL
          value: ""
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 30
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: kfc-dev
  labels:
    app: kfc-frontend
spec:
  selector:
    app: kfc-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: LoadBalancer